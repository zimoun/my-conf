#!/bin/bash

guix pull --commit=$(git -C $GUIX_MYSRC                                  \
                         log                                             \
                         --before=$(date --date='2 weeks ago' +%Y-%m-%d) \
                         --format="%h" | head -n1)

echo "done."
exit 0
